# Copy to DEF-YYYY-MM-DD-NNN.yaml. Filed by the finder; routed/closed by Librarian.
id: DEF-YYYY-MM-DD-NNN
project:
class:              # F1-F5 | C1-C4 | X1-X4 | J1-J4 | P1-P3 | E1-E3 (spec §1.2)
severity:           # SEV1-SEV4 (spec §1.3)
stage_detected:     # S0-S4
stage_should_have:  # earliest realistic catch point, e.g. S0-scout
owner_agent:        # scout | builder | verifier | red-team | librarian | mechanic | spec
model:              # model in use when introduced
description: >
evidence:           # link/ref to the artifact that proves it
root_cause: >
lesson_file:        # path written by Librarian
regression_artifact: # the check that would have caught it (test > rubric line > checklist)
promotion: memory   # memory | hard-rule | gate
recurrence_of: null # DEF id if repeat — triggers promotion ladder
model_specific: false
status: open        # open | closed (closed requires lesson_file + regression_artifact)
