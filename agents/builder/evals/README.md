# builder — regression benchmark

Empty by design. Cases are **grown from defects**, never invented upfront — a pre-written benchmark tests imagined failure modes rather than real ones.

Every closed defect record contributes its regression artifact as a case here. Expect 5–10 earned cases within three projects.

Once this benchmark holds 3 or more cases, every edit to `../agent.md` must re-run it first and attach before/after results; an edit that degrades the benchmark is reverted. Below 3 cases, edits land on Librarian review alone (bootstrap exception).

| Case | Origin defect | Check | Pass condition |
|---|---|---|---|
| _(none yet)_ | | | |
